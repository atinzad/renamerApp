[project]
name = "renamerapp"
version = "0.1.0"
description = "Google Drive Image Renamer (Increment 1)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "keyring>=25.0.0",
    "requests>=2.31.0",
    "streamlit>=1.30.0",
]
